namespace Saga.Data
{
    /// <summary>Sprint 7 tunables for the modular sprite + equipment system.</summary>
    public static class EquipmentConstants
    {
        /// <summary>Id of the default Body layer set every character starts with.</summary>
        public const string DefaultBodyId = "body_chibi_neutral";

        /// <summary>Default seconds-per-frame for SpriteLayerSet animations when no override is set.</summary>
        public const float DefaultFrameDuration = 0.1f;
    }
}
